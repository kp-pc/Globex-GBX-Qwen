<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude sensitive data from full backup -->
    <exclude domain="sharedpref" path="wallet_prefs.xml"/>
    <exclude domain="database" path="globex.db"/>
    <exclude domain="file" path="keystore"/>
</full-backup-content>
